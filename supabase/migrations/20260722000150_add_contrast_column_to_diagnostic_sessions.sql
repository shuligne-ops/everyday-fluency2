-- Догоняющая миграция: колонка contrast уже была применена в прод напрямую
-- (версия 20260722051728 в supabase_migrations.schema_migrations), но файла в
-- репозитории не было — при этом в неё пишут четыре роута разбора.
-- Здесь фиксируется фактическое состояние прода. Применять повторно не нужно.
--
-- Известный долг: номера версий в базе и имена файлов в этой папке не совпадают
-- ни по одной миграции. Выравнивание — отдельная задача; `supabase db push`
-- до него запускать нельзя, он попытается накатить существующие миграции заново.
alter table public.diagnostic_sessions
  add column if not exists contrast jsonb null;
